"""CommandRegistry parse + engine command handling (replaces legacy CLI-input tests)."""

# Kept as a lightweight regression for command parsing; interactive prompt
# handling lives in engine/repl.py and is exercised via test_commands.py.
